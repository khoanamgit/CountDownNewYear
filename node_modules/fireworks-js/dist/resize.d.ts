import type { Options } from './options.js';
export declare class Resize {
    private readonly options;
    private readonly updateSize;
    constructor(options: Options, updateSize: () => void);
    mount(): void;
    unmount(): void;
    private handleResize;
}
